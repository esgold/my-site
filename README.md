Erin — Designer (HTML-only personal site)

Open `index.html` in a browser to view a minimal, styled personal site for Erin.

Files added:
- `index.html` — main page
- `css/styles.css` — styles

No build step required. To preview locally, open the file in your browser or run a simple local server:

```bash
# macOS / zsh
python3 -m http.server 8000
# then open http://localhost:8000
```
